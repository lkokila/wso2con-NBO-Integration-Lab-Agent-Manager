# Single Ballerina package at the repo root; this is the only .gitignore.

# Build output
target/
generated/

# Configuration holding credentials - supplied via Agent Manager env vars
# at deploy time, never committed. See README.md "Configuration".
Config.toml
**/Config.toml

# Development-time tracing
trace_enabled.bal

# Local editor / OS state
.vscode/
.DS_Store

# Empty stubs the Ballerina AI tooling generates on build
agents.bal
functions.bal
automation.bal
data_mappings.bal
connections.bal
